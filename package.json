{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare-client":"cd teams-clone && npm install && npm run build",
    "prepare-server":"npm install",
    "heroku-postbuild": "npm run prepare-client && npm run prepare-server",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "redis": "^3.1.2",
    "socket.io": "^2.3.0"
  }
}
